# Data Structures and Algorithms

Notes following the six-course DSA specialization from UCSD: foundations,
data structures, graphs, strings, advanced algorithms, and a genome-assembly
capstone.

Ideas behind solutions are generally mathematically motivated (or an attempt at
it).

Most code appears inline as `lstlisting` blocks, with the actual code files in
another repo (`dsa-ucsd`).
